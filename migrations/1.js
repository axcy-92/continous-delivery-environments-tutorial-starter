module.exports = function runMigration(migration) {
    /*
    The empty migration to represent the initial import that we did manually by command:
        contentful space import --content-file .\import\export.json
    */
    return;
};